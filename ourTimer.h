/* User can only call these functions.
 * They know nothing of the implementation.
 * Only rules:
 * call startTime.
 * timeSince will return to you the seconds since startTime
 */

void startTimer();
float timeSince();
